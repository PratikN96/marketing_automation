# Project: marketing_automation

## Rules

### Sensitive files — never read
Do NOT read, cat, or access any of these files under any circumstance:
- `credentials.json`
- `token.json`
- `.env`
- Any file containing API keys, secrets, or tokens

If a task needs values from these files, ask the user to provide them directly.
